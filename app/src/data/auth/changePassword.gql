mutation changePassword($email: String!, $currentPassword: String!, $newPassword: String!) {
  userChangePass(
    input: { email: $email, oldPassword: $currentPassword, newPassword: $newPassword }
  ) {
    bigInt
  }
}
