---
import DownloadPlatform from "~/components/DownloadPlatform.astro";
import { langs } from "~/i18n";

export const getStaticPaths = () => {
  return langs.map((lang) => ({ params: { lang } }));
};
---

<DownloadPlatform lang={Astro.params.lang} platform="linux" />
