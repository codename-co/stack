---
import { getLangFromUrl } from "~/i18n";
import LanguagePicker from "./LanguagePicker.astro";

const currentLang = getLangFromUrl(Astro.url);
---

<nav
  id="navbar"
  class="flex items-center justify-between flex-wrap w-full mx-auto"
>
  <a
    class="flex items-center flex-shrink-0 mr-6"
    href={`/${currentLang === "en" ? "" : currentLang}`}
  >
    <img src="/128x128.png" alt="" width="32" height="32" class="mr-2" />
    <span class="font-semibold text-xl tracking-tight text-black">Stack</span>
  </a>
  <div class="h-10 grid grid-flow-col text-base">
    <LanguagePicker />
  </div>
  <!-- <div class="block lg:hidden">
    <button
      class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white"
    >
      <svg
        class="fill-current h-3 w-3"
        viewBox="0 0 20 20"
        xmlns="http://www.w3.org/2000/svg"
      >
        <title>Menu</title>
        <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
      </svg>
    </button>
  </div> -->
</nav>
